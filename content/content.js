/**
 * @file content.js
 * @description In der neuen Version dient dieses Skript hauptsächlich als Platzhalter.
 * Die primäre kosmetische Blockierung erfolgt über die injizierte 'cosmetic_filters.css'.
 * @version 4.0.0
 * @author Gemini
 */
(() => {
    'use strict';
    // Dieses Skript bleibt für zukünftige Erweiterungen erhalten, z.B.
    // um auf sehr komplexe, Skript-basierte Anzeigen zu reagieren, die
    // nicht allein durch CSS blockiert werden können.
    // In der aktuellen, auf Geschwindigkeit optimierten Version
    // ist keine aktive Logik erforderlich, da die CSS-Datei die Arbeit erledigt.
    console.log("Pagy Blocker Content Script geladen. Blockierung via CSS.");
})();